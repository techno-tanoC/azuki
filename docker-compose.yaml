version: '3.8'

services:
  client:
    build:
      context: ./
      dockerfile: docker/client/Dockerfile
    ports:
      - "3000:3000"

  server:
    build:
      context: ./
      dockerfile: docker/server/Dockerfile
    ports:
      - "8080:8080"
    environment:
      AZUKI_STORAGE_PATH: /volume
    volumes:
      - type: bind
        source: ./volume
        target: /volume
